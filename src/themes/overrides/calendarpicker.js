export default function CalendarPicker(theme) {
    return {
        MuiCalendarPicker: {
            styleOverrides: {
                root: {
                    width: '100%',
                    overflow: 'visible',
                },
            },
        },
    };
}