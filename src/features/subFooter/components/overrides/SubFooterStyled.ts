//component styles
import { styled } from "@mui/system";
import { AppBar } from "@mui/material";

const SubFooterStyled = styled(AppBar)(({ theme }) => ({
    border: "none",

}));

export default SubFooterStyled
