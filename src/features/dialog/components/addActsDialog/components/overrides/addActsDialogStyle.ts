import { Stack } from '@mui/material'
import { styled } from '@mui/material/styles';

const AddActsDialogStyle = styled(Stack)(({ theme }) => ({
    margin: 'auto',
}));
export default AddActsDialogStyle